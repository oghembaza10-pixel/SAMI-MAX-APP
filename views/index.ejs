<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SAMII — Automatisation Intelligente pour E-commerçants</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700;800;900&family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-deep: #030407;
            --bg-surface: rgba(12, 16, 28, 0.8);
            --bg-panel: rgba(255, 255, 255, 0.035);
            --bg-panel-strong: rgba(255, 255, 255, 0.065);
            --gold: #C5A059;
            --gold-hover: #E8C27A;
            --gold-glow: rgba(197, 160, 89, 0.35);
            --cyan: #5FD4FF;
            --success: #3ddc84;
            --text-main: #F1F0EC;
            --text-muted: #838a99;
            --border-soft: 1px solid rgba(255, 255, 255, 0.08);
            --border-gold: 1px solid rgba(197, 160, 89, 0.4);
            --font-display: 'Cinzel', serif;
            --font-body: 'Inter', sans-serif;
            --font-mono: 'JetBrains Mono', monospace;
            --ease: cubic-bezier(0.16, 1, 0.3, 1);
            --shadow-luxury: 0 30px 60px rgba(0, 0, 0, 0.7), inset 0 1px 0 rgba(197, 160, 89, 0.2);
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }
        html { scroll-behavior: smooth; -webkit-text-size-adjust: 100%; }
        body {
            background: var(--bg-deep);
            color: var(--text-main);
            font-family: var(--font-body);
            min-height: 100vh;
            width: 100%;
            overflow-x: hidden;
            position: relative;
        }

        /* --- FOND GÉODÉSIQUE & GLOBE D'OR CENTRAL --- */
        .world-bg { position: fixed; inset: 0; z-index: -3; overflow: hidden; pointer-events: none; }
        .world-bg__overlay {
            position: absolute; inset: 0;
            background: 
                radial-gradient(circle at 50% 30%, rgba(197, 160, 89, 0.15), transparent 60%),
                radial-gradient(circle at 50% 100%, rgba(95, 212, 255, 0.05), transparent 70%),
                linear-gradient(180deg, rgba(3, 4, 7, 0.7) 0%, var(--bg-deep) 90%);
        }
        .world-svg { position: absolute; top: 45%; left: 50%; transform: translate(-50%, -50%); width: min(1400px, 220vw); height: auto; opacity: .6; }
        .globe-ring { fill: none; stroke: rgba(197, 160, 89, 0.2); stroke-width: 1.2; }
        .globe-ring--gold { stroke: rgba(197, 160, 89, 0.35); stroke-dasharray: 6 6; }
        .globe-meridian { fill: none; stroke: rgba(197, 160, 89, 0.08); stroke-width: 1; }
        .gold-sphere { fill: url(#goldSphereGrad); filter: drop-shadow(0 0 35px rgba(197, 160, 89, 0.6)); }
        .hub-dot { fill: #fff; filter: drop-shadow(0 0 10px rgba(255, 255, 255, 0.9)); }
        .hub-pulse { fill: none; stroke: var(--gold); stroke-width: 1.5; opacity: 0; transform-origin: center; animation: pulse-ring 4s cubic-bezier(0.1, 0.8, 0.3, 1) infinite; }
        @keyframes pulse-ring {
            0% { opacity: .9; transform: scale(0.2); }
            100% { opacity: 0; transform: scale(3.5); }
        }
        .arc-line { fill: none; stroke: url(#arcGoldGradient); stroke-width: 1.8; stroke-dasharray: 10 6; opacity: .8; animation: arc-flow 4s linear infinite; }
        @keyframes arc-flow { to { stroke-dashoffset: -120; } }
        .world-rotate { animation: world-spin 120s linear infinite; transform-origin: center; }
        @keyframes world-spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

        .bg-particles {
            position: fixed; inset: 0; z-index: -2; pointer-events: none; opacity: .25;
            background-image: radial-gradient(circle, rgba(197,160,89,0.5) 1.5px, transparent 1.5px), radial-gradient(circle, rgba(255,255,255,0.3) 1px, transparent 1px);
            background-size: 180px 180px, 260px 260px;
            animation: particles-drift 60s linear infinite;
        }
        @keyframes particles-drift { from { background-position: 0 0, 0 0; } to { background-position: 0 -600px, 0 -800px; } }

        /* --- TOP BAR --- */
        .top-bar {
            position: sticky; top: 0; z-index: 1000;
            display: flex; justify-content: space-between; align-items: center;
            padding: 16px 24px; backdrop-filter: blur(28px);
            background: rgba(3, 4, 7, 0.9); border-bottom: var(--border-soft);
            width: 100%;
        }
        .brand { display: flex; align-items: center; gap: 10px; font-family: var(--font-display); font-weight: 800; color: #fff; font-size: 1.15rem; letter-spacing: 0.1em; text-decoration: none; }
        .brand span { background: linear-gradient(135deg, #fff 30%, var(--gold)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        
        .top-bar__right { display: flex; align-items: center; gap: 16px; }
        .ecosystem-links { display: none; gap: 12px; }
        @media (min-width: 1024px) { .ecosystem-links { display: flex; } }
        .eco-link {
            padding: 7px 14px; border-radius: 20px; text-decoration: none; font-size: .78rem; font-weight: 600;
            color: var(--text-muted); border: 1px solid rgba(255,255,255,0.08); transition: all .25s ease;
        }
        .eco-link:hover { color: var(--gold); border-color: rgba(197,160,89,0.5); background: rgba(197,160,89,0.06); }
        
        .lang-switch { display: flex; gap: 3px; font-family: var(--font-mono); font-size: .7rem; background: rgba(0,0,0,0.5); padding: 3px; border-radius: 8px; border: var(--border-soft); }
        .lang-switch span { padding: 5px 8px; border-radius: 6px; cursor: pointer; color: var(--text-muted); transition: all .2s ease; font-weight: 600; }
        .lang-switch span.active, .lang-switch span:hover { color: var(--gold); background: rgba(197,160,89,0.15); }

        /* --- HERO SECTION --- */
        .hero { max-width: 860px; margin: 0 auto; padding: 60px 20px 30px; text-align: center; }
        .eyebrow {
            font-family: var(--font-mono); letter-spacing: .18em; font-size: .72rem; color: var(--gold);
            text-transform: uppercase; display: inline-flex; align-items: center; gap: 8px; margin-bottom: 20px;
            padding: 6px 16px; border-radius: 20px; background: rgba(197,160,89,0.08); border: 1px solid rgba(197,160,89,0.3);
        }
        .eyebrow .dot { width: 6px; height: 6px; border-radius: 50%; background: var(--success); box-shadow: 0 0 10px var(--success); animation: blink 2s infinite; }
        @keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0.4; } }
        
        .hero h1 {
            font-family: var(--font-display); font-size: clamp(2.2rem, 7vw, 4rem); color: #fff; line-height: 1.15;
            margin-bottom: 20px; font-weight: 800; letter-spacing: -0.01em;
            text-shadow: 0 0 50px rgba(197,160,89,0.35);
        }
        .hero h1 span { background: linear-gradient(135deg, #fff 20%, var(--gold) 90%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .hero p { color: var(--text-muted); font-size: 1.05rem; line-height: 1.7; padding: 0 10px; max-width: 640px; margin: 0 auto 36px; }

        .live-stats { display: flex; justify-content: center; gap: 32px; flex-wrap: wrap; margin-top: 10px; padding: 0 16px; }
        .live-stat { text-align: center; background: var(--bg-panel); border: var(--border-soft); padding: 14px 24px; border-radius: 14px; min-width: 140px; backdrop-filter: blur(10px); }
        .live-stat__num { font-family: var(--font-mono); font-weight: 700; font-size: 1.5rem; color: var(--gold); text-shadow: 0 0 15px rgba(197,160,89,0.4); }
        .live-stat__label { font-family: var(--font-mono); font-size: .65rem; color: var(--text-muted); text-transform: uppercase; letter-spacing: .08em; margin-top: 4px; }

        /* --- AUTH ZONE (FORMULAIRE HAUT DE GAMME) --- */
        .auth-zone { max-width: 500px; margin: 40px auto 0; padding: 0 20px; }
        .auth-tabs {
            display: flex; gap: 6px; margin-bottom: 18px;
            background: rgba(0, 0, 0, 0.4); border-radius: 14px; padding: 5px; border: var(--border-soft);
        }
        .auth-tab {
            flex: 1; padding: 12px 10px; border-radius: 10px; border: none; background: transparent;
            color: var(--text-muted); font-family: var(--font-body); font-weight: 700; font-size: .86rem; cursor: pointer;
            transition: all .3s var(--ease);
        }
        .auth-tab.active { background: linear-gradient(135deg, var(--gold), var(--gold-hover)); color: #000; box-shadow: 0 4px 20px rgba(197,160,89,0.3); }

        .auth-card {
            position: relative; overflow: hidden;
            background: var(--bg-surface); backdrop-filter: blur(30px); border: var(--border-gold);
            border-radius: 24px; padding: 30px 24px; box-shadow: var(--shadow-luxury);
        }
        .auth-card::before {
            content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1.5px;
            background: linear-gradient(90deg, transparent, var(--gold), transparent);
        }

        .step-indicator { display: flex; align-items: center; gap: 8px; margin-bottom: 20px; }
        .step-dot { flex: 1; height: 3px; border-radius: 3px; background: rgba(255, 255, 255, 0.1); transition: background .4s ease; }
        .step-dot.done { background: var(--gold); box-shadow: 0 0 10px rgba(197,160,89,0.6); }
        .step-label { font-family: var(--font-mono); font-size: .68rem; color: var(--gold); text-align: center; margin-bottom: 18px; letter-spacing: .06em; text-transform: uppercase; }

        .form-step { display: none; }
        .form-step.active { display: block; animation: step-in .4s var(--ease) both; }
        @keyframes step-in { from { opacity: 0; transform: translateY(15px); } to { opacity: 1; transform: translateY(0); } }

        .type-choice { display: flex; gap: 10px; margin-bottom: 20px; }
        .type-choice label {
            flex: 1; display: flex; flex-direction: column; align-items: center; gap: 6px;
            padding: 16px 10px; border-radius: 14px; cursor: pointer;
            border: 1px solid rgba(255, 255, 255, 0.08); background: rgba(0, 0, 0, 0.25);
            font-size: .8rem; color: var(--text-muted); transition: all .3s ease;
        }
        .type-choice input { display: none; }
        .type-choice label:has(input:checked) {
            border-color: var(--gold); background: rgba(197, 160, 89, 0.12); color: #fff;
            box-shadow: 0 0 25px rgba(197, 160, 89, 0.25);
        }
        .type-choice .icon { font-size: 1.5rem; }

        .field-group { margin-bottom: 16px; }
        .field-group label {
            display: block; font-family: var(--font-mono); font-size: .68rem; text-transform: uppercase;
            letter-spacing: .06em; color: var(--text-muted); margin-bottom: 6px;
        }
        .field-row { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
        .auth-card input, .auth-card select {
            width: 100%; padding: 13px 16px; border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.1); background: rgba(0, 0, 0, 0.45);
            color: var(--text-main); font-size: 16px; font-family: var(--font-body);
            transition: all .3s ease;
        }
        .auth-card select option { background: #0a0d14; color: #fff; }
        .auth-card input:focus, .auth-card select:focus {
            outline: none; border-color: var(--gold);
            box-shadow: 0 0 0 3px rgba(197, 160, 89, 0.15), 0 0 25px rgba(197, 160, 89, 0.25);
            background: rgba(0, 0, 0, 0.6);
        }

        #champ-autre-pays { display: none; margin-top: 10px; }

        .grade-preview {
            display: flex; align-items: center; gap: 12px; margin-top: 6px; margin-bottom: 18px;
            padding: 12px 16px; border-radius: 12px;
            background: rgba(197, 160, 89, 0.08); border: 1px solid rgba(197, 160, 89, 0.25);
        }
        .grade-preview__icon { font-size: 1.5rem; }
        .grade-preview__text { font-size: .78rem; color: var(--text-muted); line-height: 1.45; }
        .grade-preview__text strong { color: var(--gold); }

        .btn-row { display: flex; gap: 10px; margin-top: 10px; }
        .auth-btn-back {
            padding: 14px 18px; border-radius: 12px; border: 1px solid rgba(255, 255, 255, 0.12);
            background: transparent; color: var(--text-muted); font-weight: 700; font-size: .84rem; cursor: pointer;
            transition: all .25s ease;
        }
        .auth-btn-back:hover { color: #fff; border-color: rgba(255,255,255,0.3); background: rgba(255,255,255,0.03); }

        .auth-submit {
            flex: 1; padding: 14px; border: none; border-radius: 12px; position: relative; overflow: hidden;
            background: linear-gradient(135deg, var(--gold), var(--gold-hover)); color: #000;
            font-weight: 800; font-size: .92rem; cursor: pointer; font-family: var(--font-display);
            transition: transform .2s ease, box-shadow .2s ease;
            box-shadow: 0 4px 20px rgba(197,160,89,0.3);
        }
        .auth-submit:hover { transform: translateY(-2px); box-shadow: 0 6px 25px rgba(197,160,89,0.5); }
        
        .glow-follow {
            position: absolute; width: 120px; height: 120px;
            background: radial-gradient(circle, rgba(255,255,255,0.5), transparent 70%);
            border-radius: 50%; pointer-events: none; opacity: 0; transition: opacity .2s ease;
            transform: translate(-50%, -50%);
        }
        .auth-submit:hover .glow-follow { opacity: 1; }

        .auth-msg { text-align: center; margin-top: 14px; font-size: .82rem; color: #ff6b6b; min-height: 20px; font-family: var(--font-mono); }
        .auth-msg.ok { color: var(--success); }
        .forgot-link { display: block; text-align: right; font-size: .76rem; color: var(--text-muted); text-decoration: none; margin-top: -6px; margin-bottom: 14px; transition: color .2s; }
        .forgot-link:hover { color: var(--gold); }

        /* --- SECTION AUTOMATISATION & CANAUX CLIENTS --- */
        .automation-section { max-width: 1100px; margin: 80px auto 0; padding: 0 20px; }
        .section-title {
            text-align: center; font-family: var(--font-mono); font-size: .72rem; letter-spacing: .16em;
            text-transform: uppercase; color: var(--gold); margin-bottom: 8px;
        }
        .section-subtitle { text-align: center; font-family: var(--font-display); font-size: clamp(1.4rem, 4vw, 2rem); color: #fff; margin-bottom: 32px; font-weight: 700; }
        
        .automation-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 14px; }
        @media (min-width: 768px) { .automation-grid { grid-template-columns: repeat(4, 1fr); gap: 18px; } }
        
        .automation-card {
            background: var(--bg-panel); border: var(--border-soft); border-radius: 18px;
            padding: 24px 18px; text-align: center; transition: all .35s var(--ease); backdrop-filter: blur(12px);
            position: relative; overflow: hidden;
        }
        .automation-card::after {
            content: ''; position: absolute; inset: 0; background: linear-gradient(135deg, rgba(197,160,89,0.08), transparent);
            opacity: 0; transition: opacity .3s ease; pointer-events: none;
        }
        .automation-card:hover { transform: translateY(-6px); border-color: rgba(197, 160, 89, 0.5); background: var(--bg-panel-strong); box-shadow: 0 15px 35px rgba(0,0,0,0.5); }
        .automation-card:hover::after { opacity: 1; }
        .automation-card__icon { font-size: 2.2rem; margin-bottom: 12px; filter: drop-shadow(0 0 10px rgba(197,160,89,0.4)); }
        .automation-card__name { font-size: .95rem; font-weight: 700; color: #fff; margin-bottom: 6px; font-family: var(--font-display); }
        .automation-card__desc { font-size: .75rem; color: var(--text-muted); line-height: 1.5; }
        .automation-badge {
            display: inline-block; margin-top: 14px; font-size: .65rem; font-family: var(--font-mono);
            padding: 3px 12px; border-radius: 20px; background: rgba(61, 220, 132, 0.12); color: var(--success);
            border: 1px solid rgba(61, 220, 132, 0.25);
        }

        /* --- FOOTER --- */
        .site-footer {
            border-top: var(--border-soft); padding: 32px 20px; margin-top: 80px;
            display: flex; flex-direction: column; align-items: center; gap: 18px; text-align: center;
            background: rgba(3, 4, 7, 0.95); backdrop-filter: blur(20px);
        }
        @media (min-width: 768px) { .site-footer { flex-direction: row; justify-content: space-between; padding: 36px 40px; } }
        .site-footer__quote { font-family: var(--font-mono); color: var(--text-muted); font-size: .72rem; letter-spacing: 0.05em; }
        .site-footer__meta { font-family: var(--font-mono); font-size: .68rem; color: var(--text-muted); }

        @media (min-width: 768px) {
            .top-bar { padding: 18px 40px; }
            .brand { font-size: 1.25rem; }
            .auth-card { padding: 36px; border-radius: 28px; }
        }
    </style>
</head>
<body>

    <!-- Fond Géodésique & Globe d'or central -->
    <div class="world-bg">
        <div class="world-bg__overlay"></div>
        <svg class="world-svg" viewBox="0 0 800 800" xmlns="http://www.w3.org/2000/svg">
            <defs>
                <linearGradient id="goldSphereGrad" x1="0%" y1="0%" x2="100%" y2="100%">
                    <stop offset="0%" stop-color="#E8C27A"/>
                    <stop offset="50%" stop-color="#C5A059"/>
                    <stop offset="100%" stop-color="#5a451d"/>
                </linearGradient>
                <linearGradient id="arcGoldGradient" x1="0%" y1="0%" x2="100%" y2="100%">
                    <stop offset="0%" stop-color="#C5A059"/>
                    <stop offset="100%" stop-color="#E8C27A"/>
                </linearGradient>
            </defs>
            <g class="world-rotate">
                <circle class="globe-ring" cx="400" cy="400" r="320"/>
                <circle class="globe-ring globe-ring--gold" cx="400" cy="400" r="250"/>
                <circle class="globe-ring" cx="400" cy="400" r="180"/>
                <ellipse class="globe-meridian" cx="400" cy="400" rx="320" ry="90"/>
                <ellipse class="globe-meridian" cx="400" cy="400" rx="320" ry="190"/>
                <ellipse class="globe-meridian" cx="400" cy="400" rx="90" ry="320"/>
                <ellipse class="globe-meridian" cx="400" cy="400" rx="190" ry="320"/>
            </g>
            <!-- Globe central en or -->
            <circle class="gold-sphere" cx="400" cy="400" r="75"/>
            <!-- Points de connexion rayonnants -->
            <circle class="hub-dot" cx="400" cy="270" r="5"/>
            <circle class="hub-pulse" cx="400" cy="270" r="5"/>
            <circle class="hub-dot" cx="550" cy="330" r="4.5"/>
            <circle class="hub-pulse" cx="550" cy="330" r="4.5"/>
            <circle class="hub-dot" cx="500" cy="520" r="4.5"/>
            <circle class="hub-pulse" cx="500" cy="520" r="4.5"/>
            <circle class="hub-dot" cx="280" cy="370" r="4.5"/>
            <circle class="hub-pulse" cx="280" cy="370" r="4.5"/>
            <path class="arc-line" d="M400,400 Q400,335 400,270"/>
            <path class="arc-line" d="M400,400 Q475,365 550,330"/>
            <path class="arc-line" d="M400,400 Q450,460 500,520"/>
            <path class="arc-line" d="M400,400 Q340,385 280,370"/>
        </svg>
    </div>
    <div class="bg-particles"></div>

    <!-- Barre de Navigation -->
    <header class="top-bar">
        <a href="/" class="brand">
            <span>SAMII · AUTOMATION</span>
        </a>
        <div class="top-bar__right">
            <nav class="ecosystem-links">
                <a href="#solutions" class="eco-link">Solutions</a>
                <a href="#features" class="eco-link">Canaux</a>
                <a href="#contact" class="eco-link">Contact</a>
            </nav>
            <div class="lang-switch">
                <span data-lang="fr" class="active">FR</span>
                <span data-lang="en">EN</span>
                <span data-lang="ar">AR</span>
            </div>
        </div>
    </header>

    <!-- Section Hero -->
    <section class="hero">
        <div class="eyebrow"><span class="dot"></span>SAMII OS · Cloud Infrastructure Render</div>
        <h1>Automatisez la relation client<br>de vos <span>E-commerçants.</span></h1>
        <p>Propulsez l'activité de vos clients en connectant instantanément leurs boutiques et leurs réseaux sociaux pour automatiser le support, les commandes et l'acquisition.</p>

        <div class="live-stats">
            <div class="live-stat">
                <div class="live-stat__num" id="stat-marchands">1,482</div>
                <div class="live-stat__label">Boutiques gérées</div>
            </div>
            <div class="live-stat">
                <div class="live-stat__num" id="stat-pays">34</div>
                <div class="live-stat__label">Pays connectés</div>
            </div>
            <div class="live-stat">
                <div class="live-stat__num" id="stat-samii">24/7</div>
                <div class="live-stat__label">Agent Actif</div>
            </div>
        </div>
    </section>

    <!-- Zone d'Authentification / Inscription -->
    <section class="auth-zone">
        <div class="auth-tabs">
            <button type="button" class="auth-tab active" data-tab="register">Créer un compte</button>
            <button type="button" class="auth-tab" data-tab="login">Se connecter</button>
        </div>

        <div class="auth-card">
            <!-- Formulaire d'Inscription -->
            <form id="form-register">
                <div class="step-indicator">
                    <div class="step-dot done" data-step-dot="1"></div>
                    <div class="step-dot" data-step-dot="2"></div>
                </div>
                <div class="step-label" id="step-label">Étape 1 sur 2 — Informations Générales</div>

                <!-- Étape 1 -->
                <div class="form-step active" data-step="1">
                    <div class="type-choice">
                        <label>
                            <input type="radio" name="type_compte" value="client" checked>
                            <span class="icon">👤</span>
                            <span>Particulier</span>
                        </label>
                        <label>
                            <input type="radio" name="type_compte" value="agence">
                            <span class="icon">💼</span>
                            <span>Agence / Pro</span>
                        </label>
                    </div>

                    <div class="field-row">
                        <div class="field-group">
                            <label>Nom</label>
                            <input name="nom" placeholder="Votre nom" required>
                        </div>
                        <div class="field-group">
                            <label>Prénom</label>
                            <input name="prenom" placeholder="Votre prénom" required>
                        </div>
                    </div>

                    <div class="field-group">
                        <label>Adresse Email</label>
                        <input name="email" type="email" placeholder="votre@email.com" required>
                    </div>

                    <div class="field-row">
                        <div class="field-group">
                            <label>Téléphone</label>
                            <input name="telephone" placeholder="+33 6..." required>
                        </div>
                        <div class="field-group">
                            <label>Pays de Résidence</label>
                            <select name="pays" id="select-pays">
                                <option value="FR" selected>France</option>
                                <option value="DZ">Algérie</option>
                                <option value="MA">Maroc</option>
                                <option value="TN">Tunisie</option>
                                <option value="AE">Émirats Arabes Unis</option>
                                <option value="CA">Canada</option>
                                <option value="autre">🌐 Autre pays...</option>
                            </select>
                            <input type="text" id="champ-autre-pays" name="pays_autre" placeholder="Précisez votre pays">
                        </div>
                    </div>

                    <div class="grade-preview">
                        <span class="grade-preview__icon">⚡</span>
                        <span class="grade-preview__text">Statut : <strong>Partenaire Certifié SAMII</strong> — Accès direct aux automatisations e-commerce.</span>
                    </div>

                    <button type="button" class="auth-submit" id="btn-step1-next">
                        Continuer vers la sécurité →
                        <span class="glow-follow"></span>
                    </button>
                </div>

                <!-- Étape 2 -->
                <div class="form-step" data-step="2">
                    <div class="field-group">
                        <label>Mot de passe sécurisé</label>
                        <input name="password" type="password" placeholder="Minimum 6 caractères" required minlength="6">
                    </div>

                    <div class="btn-row" style="margin-top: 24px;">
                        <button type="button" class="auth-btn-back" id="btn-step2-back">← Retour</button>
                        <button type="submit" class="auth-submit">
                            Lancer mon Compte 🚀
                            <span class="glow-follow"></span>
                        </button>
                    </div>
                </div>

                <div class="auth-msg" id="register-msg"></div>
            </form>

            <!-- Formulaire de Connexion -->
            <form id="form-login" style="display:none;">
                <div class="field-group">
                    <label>Adresse Email</label>
                    <input name="email" type="email" placeholder="votre@email.com" required>
                </div>

                <div class="field-group">
                    <label>Mot de passe</label>
                    <input name="password" type="password" placeholder="Votre mot de passe" required>
                </div>

                <a href="/forgot-password" class="forgot-link">Mot de passe oublié ?</a>

                <button type="submit" class="auth-submit" style="width:100%;">
                    Se connecter à SAMII OS ⚡
                    <span class="glow-follow"></span>
                </button>

                <div class="auth-msg" id="login-msg"></div>
            </form>
        </div>
    </section>

    <!-- Grille des Canaux Automatisés -->
    <section class="automation-section" id="features">
        <div class="section-title">Canaux d'Automatisation</div>
        <h2 class="section-subtitle">Connectez vos clients partout où ils vendent</h2>
        <div class="automation-grid">
            <div class="automation-card">
                <div class="automation-card__icon">💬</div>
                <div class="automation-card__name">WhatsApp Business</div>
                <div class="automation-card__desc">Gestion automatique des commandes, relances paniers abandonnés et support 24/7.</div>
                <span class="automation-badge">Actif</span>
            </div>
            <div class="automation-card">
                <div class="automation-card__icon">✈️</div>
                <div class="automation-card__name">Telegram Bot</div>
                <div class="automation-card__desc">Notifications instantanées et tunnels de vente automatisés pour vos clients.</div>
                <span class="automation-badge">Ultra-rapide</span>
            </div>
            <div class="automation-card">
                <div class="automation-card__icon">▶️</div>
                <div class="automation-card__name">YouTube & Google</div>
                <div class="automation-card__desc">Acquisition de trafic qualifié et synchronisation des campagnes publicitaires.</div>
                <span class="automation-badge">Optimisé</span>
            </div>
            <div class="automation-card">
                <div class="automation-card__icon">☁️</div>
                <div class="automation-card__name">Render & GitHub</div>
                <div class="automation-card__desc">Hébergement cloud souverain et déploiement continu sécurisé et sans coupure.</div>
                <span class="automation-badge">Cloud Ready</span>
            </div>
        </div>
    </section>

    <!-- Pied de page -->
    <footer class="site-footer">
        <div class="site-footer__quote">SAMII OS // Cloud Infrastructure — Tous droits réservés.</div>
        <div class="site-footer__meta">Secure Protocol v4.8 (Render Deployed)</div>
    </footer>

    <!-- Scripts interactifs -->
    <script>
        const authTabs = document.querySelectorAll('.auth-tab');
        const formRegister = document.getElementById('form-register');
        const formLogin = document.getElementById('form-login');

        authTabs.forEach(tab => {
            tab.addEventListener('click', () => {
                authTabs.forEach(t => t.classList.remove('active'));
                tab.classList.add('active');
                if (tab.dataset.tab === 'register') {
                    formRegister.style.display = 'block';
                    formLogin.style.display = 'none';
                } else {
                    formRegister.style.display = 'none';
                    formLogin.style.display = 'block';
                }
            });
        });

        const btnStep1Next = document.getElementById('btn-step1-next');
        const btnStep2Back = document.getElementById('btn-step2-back');
        const step1 = document.querySelector('.form-step[data-step="1"]');
        const step2 = document.querySelector('.form-step[data-step="2"]');
        const stepDots = document.querySelectorAll('[data-step-dot]');
        const stepLabel = document.getElementById('step-label');

        btnStep1Next.addEventListener('click', () => {
            const inputsStep1 = step1.querySelectorAll('input[required], select[required]');
            let valid = true;
            inputsStep1.forEach(input => {
                if (!input.value.trim()) { valid = false; input.style.borderColor = '#ff6b6b'; }
                else { input.style.borderColor = ''; }
            });
            if (!valid) {
                document.getElementById('register-msg').innerText = "Veuillez remplir tous les champs requis.";
                return;
            }
            document.getElementById('register-msg').innerText = "";
            step1.classList.remove('active');
            step2.classList.add('active');
            stepDots[1].classList.add('done');
            stepLabel.innerText = "Étape 2 sur 2 — Sécurité & Mot de passe";
        });

        btnStep2Back.addEventListener('click', () => {
            step2.classList.remove('active');
            step1.classList.add('active');
            stepDots[1].classList.remove('done');
            stepLabel.innerText = "Étape 1 sur 2 — Informations Générales";
        });

        const selectPays = document.getElementById('select-pays');
        const champAutrePays = document.getElementById('champ-autre-pays');

        selectPays.addEventListener('change', (e) => {
            if (e.target.value === 'autre') {
                champAutrePays.style.display = 'block';
                champAutrePays.setAttribute('required', 'true');
            } else {
                champAutrePays.style.display = 'none';
                champAutrePays.removeAttribute('required');
            }
        });

        document.querySelectorAll('.auth-submit').forEach(button => {
            const glow = button.querySelector('.glow-follow');
            button.addEventListener('mousemove', (e) => {
                const rect = button.getBoundingClientRect();
                glow.style.left = `${e.clientX - rect.left}px`;
                glow.style.top = `${e.clientY - rect.top}px`;
            });
        });
    </script>
</body>
</html>
